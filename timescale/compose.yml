services:
  timescaledb:
    image: timescale/timescaledb:latest-pg17
    container_name: tottalbattle_db
    restart: always
    ports:
      - "54321:5432"
    env_file:
      - .env
    volumes:
      - ./data/tottalbattle:/var/lib/postgresql/data
